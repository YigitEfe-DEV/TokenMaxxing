[package]
name = "tokenmaxxing"
version = "0.5.0-beta"
edition = "2021"

[lib]
name = "tokenmaxxing"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
tokenmaxxing-core = { path = "../../tokenmaxxing-core" }
